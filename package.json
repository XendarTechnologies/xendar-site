{
  "name": "xendar-site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev --host",
    "dev:poll": "CHOKIDAR_USEPOLLING=1 CHOKIDAR_INTERVAL=100 astro dev --host",
    "build": "astro build",
    "preview": "astro preview",
    "perf:preview": "npm run build && astro preview --host --port 4322",
    "astro": "astro"
  },
  "dependencies": {
    "@fontsource/montserrat": "^5.2.8",
    "@tailwindcss/vite": "^4.2.1",
    "astro": "^5.17.1",
    "tailwindcss": "^4.2.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "typescript": "^5.9.3",
    "vite-plugin-compression": "^0.5.1"
  }
}
